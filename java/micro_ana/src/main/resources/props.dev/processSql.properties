shop_open_micro_sql=SELECT shopinfo.shopId as shopId, shop.name as shopName, shop.fullName as shopFullName FROM shopinfo LEFT OUTER JOIN shop ON shopinfo.shopId=shop.id WHERE shopinfo.enable_micro_mall=1 and shop.id in (SELECT businessId from account where nature='FORMAL' and businessType='SHOP');
