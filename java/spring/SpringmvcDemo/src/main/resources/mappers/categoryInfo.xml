<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.x.demo.dao.CategoryDAO">

    <insert id="addCategoryInfo" parameterType="com.x.demo.model.CategoryInfo"
            keyProperty="id">
        <![CDATA[
          insert into category (
            id,
            parent_id,
            name,
		    createDate,
		    modifyDate,
		    visible,
		    deleted
          ) values (
           #{id},
           #{parentId},
           #{name},
           #{createDate},
           #{modifyDate},
           #{visible},
           #{deleted}
          )
        ]]>
    </insert>

    <insert id="addCategoryInfoList" parameterType="java.util.List">
        insert into category (
          id,
          parent_id,
          name,
          createDate,
          modifyDate,
          visible,
          deleted
        )
        values
        <foreach collection="list" item="item" index="index" separator=",">
            (
            #{item.id},
            #{item.parentId},
            #{item.name},
            #{item.createDate},
            #{item.modifyDate},
            #{item.visible},
            #{item.deleted}
            )
        </foreach>
    </insert>
</mapper>